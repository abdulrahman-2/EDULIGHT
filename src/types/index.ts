export type Links = {
  id: number;
  label: string;
  url: string;
  icon: React.ReactElement;
};

export type DashInfo = {
  id: number;
  title: string;
  value: string;
  percentage: string;
  icon: React.ReactElement;
};
